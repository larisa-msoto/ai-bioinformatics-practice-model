# docs/

Design documents and specifications for the `model-mapper-tool` itself (its
architecture, CLI/API, output formats). **Nothing here yet.**

Not to be confused with the repo-root `../../docs/`, which is the published 4D-Bio
site. Keep tool-design writing here; keep model *knowledge* in [`../memory/`](../memory/).
